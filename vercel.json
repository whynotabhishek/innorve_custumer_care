{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "routes": [
        { "src": "/admin(.*)", "dest": "/admin.html" },
        { "src": "/((?!.*\\.).*)", "dest": "/index.html" }
    ]
}