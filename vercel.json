{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/admin(.*)", "dest": "/admin.html" },
    { "src": "/((?!.*\\.).*)", "dest": "/index.html" }
  ]
}
